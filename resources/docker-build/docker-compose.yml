services:
    emscripten-build:
        image: flobo-display-lvgl-build:latest
        build: .
        pull_policy: never
        volumes:
            - project-volume:/project
        working_dir: /project
        command: bash

volumes:
    project-volume:
        name: ${PROJECT_VOLUME:-flobo-display-lvgl-build-default}
